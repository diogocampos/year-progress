{
  "name": "year-progress",
  "private": true,
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel --no-autoinstall --open",
    "build": "rm -rf dist/* && parcel build --public-url . --no-source-maps",
    "deploy": "scripts/deploy"
  },
  "devDependencies": {
    "parcel": "^2.7.0",
    "prettier": "2.7.1"
  }
}
